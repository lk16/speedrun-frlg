# 2026-08-14 — plan.md correction pass + distance scaffolding

**Done**

- Audited `plan.md` against the decomp with a new collision-map pathfinder instead of
  object-placement greps. Corrections committed: gym-order wording (slots 2-7), Flash
  evidence (`requires_flash` exists, is render-only — `overworld.c:956`,
  `fldeff_flash.c:164`), Rock Smash resolved non-mandatory (BFS with all 15 B1F boulders
  standing), and the finding that **no starter line learns Cut+Surf+Strength**
  (`tmhm_learnsets.h`) — a solo-starter run is impossible, party floor is 2.
- Rebuilt §3 with engine-accurate sight cones (`trainer_see.c:198-231` — collision blocks
  sight). Rock Tunnel forces ≥7 fights (Ashton, Martha, Winston, Dudley, Sofia, Dana + one of
  a pair). New forced battles found: Viridian Forest Sammy, Cerulean rival, Cerulean Dig grunt
  (gives TM28), Mt Moon Super Nerd Miguel (**the fossil pickup gates the B2F exit passage** —
  previously unknown here), Tower 2F rival, Marowak (needs Silph Scope), Game Corner grunt,
  Hideout Lift-Key grunt + Giovanni, Silph 7F rival (multi-floor BFS with card-key door model:
  the 7F pad is the only entry to Giovanni's 11F region), one wild Snorlax.
- Mandatory chains added to §1: S.S. Ticket (Bill), Poké Flute (Fuji ← Marowak ← Scope ←
  Hideout ← poster grunt), Tea (Celadon → Saffron gates).
- New tool `bin/frlg-mapgraph` (stdlib BFS over `map.bin` collision + warps + connections;
  ~1 s for the full 31-leg dump) and its output `distances.json`; consequences written up in
  `route-sketch.md`: speed eras (walk 16 / run+surf 8 / bike 6 f/t,
  `event_object_movement.c:158-162,8866-8931`), Cycling-Road side favored for Fuchsia,
  Route 20 severed at Seafoam (go Pallet→Route 21 instead), Cut-before-Rock-Tunnel.

**Failed / punted**

- Per-gym forced-trainer analysis: static model breaks on script-`setmetatile` puzzles
  (Vermilion gates, Cinnabar quiz doors, Sabrina teleporters) and my auto-picked entry tiles
  were unreliable — left explicitly open in §8.3 rather than publishing shaky names.
- Victory Road distance is a floor (boulders ignored); Seafoam currents unmodeled.

**Next**

- Version × starter × second-mon sweep (§8.2/2b) once battle simulation is hooked up.
- Model VR boulder puzzles and the gym interiors; re-sweep the "believed dodgeable" list with
  full cones.
- Start pricing menus/dialogue/doors — tiles are now known, frames are not.
