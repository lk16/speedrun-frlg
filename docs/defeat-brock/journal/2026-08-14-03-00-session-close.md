# 2026-08-14 03:00 — session close: 49143 → 43308 (−11.9%), all tier-1 verified

Twenty-two full builds ran tonight (up to ten in parallel, one core each; the
checkpointed `win_battle` cut a build from ~85 to ~55 minutes). What landed, in order:

1. **seed_delay 38** (first six-candidate sweep): 49143 → 45276. The wild stream's
   rate-test sequence owns the flee count and, it turned out, Rick's dodgeability.
2. **Heal deletion**: → 44464. Seed 38 arrives at 20/23; the no-heal probe proved
   Brock falls from there (55/192 start delays).
3. **Hold-knob re-sweep on this seed** (ten variants over two waves): → **43308** at
   `turn_hold 1, text_hold 2`. The knobs were last swept on rival-1's stream; they do
   not transfer. Final table: t1-x2 43308, t2-x2 43346, t2-x1 44206, t2-x3 44244,
   t3-x2 44576, t1-x4 44083, t4-x4 44344, t2-x4 44464, t3-x4 killed while walk-looping
   at the gym door (could not have beaten the leader).

Seed waves two totals, for the record: 53 → 44673, 2 → 46592, 60 → 47045, 0 → 47714,
13 → 49274 (rescued from its width-48 fight loss by the 192-wide search, still slow),
31 → lost unhealed Brock. At text_hold 2, seeds 53 and 6 also lost unhealed Brock —
seed 38's Brock stream is unusually forgiving, which is part of why it keeps winning.

Committed code alongside the run: `Tuning::seed_delay` (title-screen idle, measured
per-frame seed map via `examples/seed-scan`), the checkpointed `win_battle` (verified
move-for-move equivalent before adoption), 192-wide en-route fight search (Rick's
optimum sat at delay 111), `examples/fight-lab` and `examples/no-heal-probe` as
reusable measurement harnesses, and the `Tuning::variants` seed-preservation fix.

## Operational notes for the next session

- Parallel builds: launch staggered (~5 s apart). Three of ~26 simultaneous launches
  died silently before writing a line; staggered relaunches all survived.
- The t3-x4 walk-loop at the Pewter gym door (round-6 forced steps, never
  progressing) is the one walker robustness bug seen tonight — `walk_fleeing`'s
  stagnation logic can spin on a town tile goal. Worth a look before the next big sweep.
- Tier-2 queue: `route-43308f-a7d7d48232c4` is the live request; the 49143, 45276 and
  44464 ones are superseded.

## What is next (the backlog stands as route.md ranks it)

The remaining big item is unchanged: the flees (~10 × ~500 frames on this stream) and
walk bloat, attackable by a model-driven path search against the known rate-test
sequence — and now also by *joint* seed × path optimisation, since builds are cheap
enough to sweep. Starter/version and IV/nature dials remain untouched.
