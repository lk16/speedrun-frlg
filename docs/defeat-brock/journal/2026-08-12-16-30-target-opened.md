# 2026-08-12 16:30 — the defeat-brock target opens

Second target started. rival-1 stands as committed (9658 frames, tier-2 passed,
`route-9658f-269d169cd6db`) and its files are frozen; everything for this target lives in
`docs/defeat-brock/` and `route/defeat-brock/`.

Decisions taken at the start, before any evidence:

- **The rival-1 prefix is a baseline, not an inheritance.** Its version/starter choice
  (LeafGreen, Bulbasaur) was scored on "frames to rival win" alone. This target's starter also
  has to beat Brock, and its PID/IVs are rolled at `givemon` — so the sweep axis grows: version
  × starter × (frames before the ball, which are now a *stat* dial, not just a battle-stream
  dial). Nothing upstream of the lab is assumed optimal.
- **Wild encounters get the frlg-battle treatment**: a pure-Rust model of the per-step
  encounter check and wild mon generation, proven roll-for-roll against libmgba before it is
  trusted to route anything. Walking the forest becomes a search over step timing/path, with
  the model pricing each candidate instead of an emulator replay per guess.
- **Semi-naive first.** One complete tier-1 run to Brock beaten before any optimisation, to get
  the segment skeleton, the ledger, and a number to beat.

In flight: three decomp research sweeps (story gates; the wild-encounter RNG pipeline;
starter/Brock data), all under the citation rule. Their findings land in
`docs/defeat-brock/route.md`'s evidence sections.
