# 2026-08-14 07:15 — tier 2 passed: the 43308 run is accepted

The host drained the verify queue this morning. `route-43308f-a7d7d48232c4` — the committed
43308-frame route — came back **pass** at 07:00 CEST: BizHawk 2.11.1 replayed all 43308
frames to the ledger's final ram_hash (`72aa2aaf…`) with the per-frame gRngValue probe
matching every frame (`verify/results/route-43308f-a7d7d48232c4.json`, fast replay, 189s).

Cross-checked before widening the ledger's claim: re-running
`frlg route export --target defeat-brock` on the committed logs reproduces the result's
`ilog_sha1` (`a7d7d48232c425d9c8e281c624b796510fe2b098`), so the pass is for exactly the
logs in `route/defeat-brock/logs/`, not some earlier revision. (The freshly exported
`.bk2`'s own sha1 differs from the queued one's — zip metadata — which is why the ilog
digest, not the bk2 hash, is the canonical identity, as `docs/rival-1/route.md` already
says.)

The superseded revisions were replayed too and also passed: 49143 (semi-naive baseline),
45276 (seed 38), 44464 (heal deleted), 43346 (text_hold 2). Nice property: every
intermediate ledger state we queued would have been accepted, so the tier-1 harness has
yet to be caught desyncing from BizHawk. One 2026-08-11 request (`route-12222f`) ended in
an `error` verdict — EmuHawk exited before the Lua reported, a runner teething problem,
not a desync — recorded here so "all passed" keeps its footnote.

Ledger, `docs/defeat-brock/route.md` and `docs/sandbox.md` (which still said tier 2 had
never completed a run) updated in this commit. Both routes are now tier-1 **and** tier-2
verified; the open items in route.md's "not optimised" list are unchanged.
