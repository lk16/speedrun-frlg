# 2026-08-15 16:10 — tier 2 passed: the 38812 run is accepted

The host drained the queue at 16:07 CEST. `route-38812f-33133637dfeb` — the committed
38812-frame route, FireRed/Squirtle on `seed_delay` 27, with the right-hand lab trigger
tile and the warp-behavior filter — came back **pass**: BizHawk 2.11.1 replayed all 38812
frames to the ledger's final ram_hash (`04f26499cb4b72abf49eddee939308ba9965f905`, which
is the `brock` segment's) with the per-frame gRngValue probe matching every frame
(`verify/results/route-38812f-33133637dfeb.json`, `fast+headless`, 67s). Queue is empty again.

Checked before widening the ledger, same as every time before: re-running
`frlg route export --target defeat-brock` on the committed logs reproduces the result's
`ilog_sha1` (`33133637dfebfc28f749ee6b5a5206fa9375e47f`), so the pass belongs to exactly the
logs in `route/defeat-brock/logs/`. The re-export's `.bk2` hashed `5b44485f…` against the
queued movie's `1af01412…` — the same zip-metadata instability `docs/rival-1/route.md`
warns about. The ilog digest is the identity; the bk2 hash only names the bytes replayed.

All 19 segments' `tier2` fields now cite this result, `route.md`'s status line and Tier 2
section say passed, `docs/sandbox.md`'s "defeat-brock's 38862" was stale, and
`docs/glitchless-run/route.md` cited 38862 as the accepted prefix in two places (the
version/starter rationale and leg 1 of the leg table).

## What the pass buys

The −50 came from two *deterministic* finds in the morning's video audit — the trigger tile
moving to (7,8) and `leg_targets` no longer walking at warp events on tiles whose metatile
behavior cannot fire them — followed by a full re-luck of every downstream stream
(`2026-08-15-15-50-right-tile-38812.md`). The re-luck is the part that could have been a
tier-1 artifact: a solver-arbitrated 2463 rival battle, a 2706 Sammy that only exists on the
`FRLG_WAIT_TO_FOREST=14` arrival, and a 3233 Brock chosen on that same shifted prefix.
BizHawk agreeing frame-for-frame on the gRngValue probe across all 38812 means the whole
re-lucked chain is real timing, not a fit against `frlg-battle`'s model.

Worth naming explicitly because the 08-15 build was the one with an *alignment scare*: the
un-door-fixed rebuild shifted the wild-LCG consumption schedule and cost +1400 in fated
flees before the fix restored it. The probe matching every frame is the strongest available
evidence that the restored alignment is the real one and not a tier-1 coincidence.

Two notes, neither about the route:

1. **67s for 38812 frames**, the third consecutive 67-68s at ~38.8k. Still not a test of the
   fps fit (19s startup + frames/790): 38812 vs 38862 vs 38950 is the same point measured
   three times, and it agreeing says the runner is stable, nothing about the curve. The cheap
   third length is still the 9658 rival-1 movie replayed headless — it has now been the
   obvious next measurement for three sessions and remains unrun.
2. **The artifacts-side Lua override is still there and still byte-identical** to
   `tools/verify-runner.lua` (`diff` clean, checked again this session) — the runner announced
   it at the top of this run, as it has since 12:08 on 08-12. The fold-back-and-delete
   condition in `docs/rival-1/route.md` has been met the whole time; deleting
   `$FRLG_ARTIFACTS/verify/verify-runner.lua` is a host-side action and is *still* pending.
   Until it happens every result line reads as if it ran on an unreviewed script.

Nothing in route.md's "not optimised" list moves. One thing in it is now dated rather than
wrong: the "zero reversal steps, zero ≥8-frame free-idle runs" audit line is `examples/
audit-run.rs` against the *38862* logs (2026-08-14). The 38812 re-roll gets its own audit
before that claim should be restated at the new number.
