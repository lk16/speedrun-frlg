# 38812: the right trigger tile, banked end-to-end

Follow-up to the morning's video-audit verdicts (`2026-08-15-10-00`): the
two deterministic finds were adopted and the whole route re-lucked on the
shifted streams. Tier-1 verified from reset; tier-2 queued as
`route-38812f-33133637dfeb`. −50 vs the accepted 38862.

## What changed

1. **Trigger tile (6,8) → (7,8)** (`segments::BATTLE_TRIGGER`): rival
   approach 4→3 steps, exit 7→6, player approach 6→5
   (`PalletTown_ProfessorOaksLab/scripts.inc`). Battle starts f7172 (was
   f7206).
2. **Warp-target filter** (`brock::leg_targets`): MapVia now targets only
   tiles whose metatile behavior actually fires a warp (0x60..0x6F). The
   lab's flanking door tiles (5,12)/(7,12) are behavior-0 floor with warp
   events; targeting (7,12) cost a measured ~120-frame fumble in the first
   right-tile build. exit-lab is 864 (was 880 on the mid tile).

## The re-luck, in order

- **Rival:** first rebuild's plain 256-delay search: 2682. squirtle-solver
  (patched to take `FRLG_LEDGER`) on the new anchor `0xbfaaed2a`: phase-2
  arbitration surfaced plan [0,25,2,13] playing **2463** real; phase 3
  found no wait 1..48 that could beat it (bars 2462−w, all empty). Seeded
  rebuild: 2463 confirmed, `stage 1: 0/256` plain delays win — same story
  as the committed 2445, unreachable without the solver.
- **Alignment scare:** the un-door-fixed rebuild (r78b) shifted the 60%
  boundary rolls, consumed a different test count upstream, and deliver/
  tutorial each caught an extra fated flee (+1400). The door fix restored
  the committed alignment — a reminder that the wild-LCG *outcomes* are
  step-indexed but the *consumption schedule* is frame-sensitive.
- **Sammy:** walled at 2872 across to-forest waits 1..8 and pre-idle
  sweeps to 96 families (6336 lines). `FRLG_WAIT_TO_FOREST=14` moved the
  arrival and Sammy dropped to **2706** (4 turns, plan [109,15]).
- **Brock:** pre-idle 3 found a **3147** on the wait-6 arrival (the
  3157-class the 08-14 backlog said needed a different arrival — it did,
  and this prefix is one); on the final wait-14 arrival the best is
  **3233** (plan [92]), and the total still wins by 50.
- **Dead dials, measured:** to-gym 1..8 (few families, none better),
  to-pewter 1..8 and the forest segment head (absorbed by the post-warp
  settle — identical totals), to-forest 9..16 except 12/14.

## Honesty notes

- 38862 remains the accepted (tier-2-passed) number until
  `route-38812f-33133637dfeb` returns a pass.
- The audit's small finds (the two ~2-step doglegs) were not "fixed"
  directly — they were realized NPC/roll costs; the rebuilt route re-rolls
  them (the new run's own audit will have its own).
- Not adopted: the low-grass deliver line (+278 frames for 0 rate tests)
  and the grass-free Route 2 stub (+32 for 0) — wired as
  `PlanRequest::test_bias` / `FRLG_TEST_BIAS_MAP_<g>_<n>` but never worth
  frames this session. Candidates if a future stream needs a cleaner
  constraint surface.
