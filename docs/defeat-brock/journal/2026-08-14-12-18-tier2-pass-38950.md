# 2026-08-14 12:18 — tier 2 passed: the 38950 run is accepted

The host drained the queue at 12:08 CEST. `route-38950f-2f221a898d8e` — the committed
38950-frame route, FireRed/Squirtle on `seed_delay` 27 — came back **pass**: BizHawk 2.11.1
replayed all 38950 frames to the ledger's final ram_hash (`b841205d77f099667cfa63b7f6de5f75b9ff1e33`,
which is the `brock` segment's) with the per-frame gRngValue probe matching every frame
(`verify/results/route-38950f-2f221a898d8e.json`, `fast+headless`, 68s). Queue is empty again.

Checked before widening the ledger, same as last time: re-running
`frlg route export --target defeat-brock` on the committed logs reproduces the result's
`ilog_sha1` (`2f221a898d8ed481a8a7ce3ecc0b5f158e6b7101`), so the pass belongs to exactly the
logs in `route/defeat-brock/logs/`. The re-export's `.bk2` hashed `b95eca59…` and a second
re-export `a251bb81…` against the queued movie's `c3f364d5…` — three different bk2 hashes for
one ilog, which is the zip-metadata instability `docs/rival-1/route.md` already warns about.
The ilog digest is the identity; the bk2 hash only names the bytes that were replayed.

All 19 segments' `tier2` fields now cite this result, `route.md`'s status line and Tier 2
section say passed, and `docs/sandbox.md`'s "defeat-brock's 43308" was stale.

Two notes from the run itself, neither about the route:

1. **68s for 38950 frames is ~573 fps**, against the 31s/~410 fps that
   `docs/rival-1/route.md`'s table records for the 9658-frame movie in the same
   `fast+headless` mode. Fitting the two points to `t = T0 + frames/r` gives **~790 fps
   steady-state behind ~19s of fixed startup** — ROM load, mono JIT, Xvfb. *Modeled from two
   measurements, not measured directly*: the honest use of it is "long movies amortize a
   fixed cost", so don't read the table's fps figures as a rate. A third length would test
   the fit cheaply, and the 43308 result cannot serve — it ran on the desktop, not headless.
2. **The artifacts-side Lua override is now byte-identical to `tools/verify-runner.lua`**
   (`diff` clean). Per the contract in `docs/rival-1/route.md`, that is the fold-back-and-delete
   condition: whatever the override was iterating on now also sits in the repo copy, so
   `$FRLG_ARTIFACTS/verify/verify-runner.lua` does nothing except print a line that makes every
   run look like it ran on an unreviewed script. Deleting it is a host-side action and not part
   of this commit.

Nothing in route.md's "not optimised" list moves — the backlog closed with the sweep
(`journal/2026-08-14-09-30-planner-tooling.md`) stands: the Sammy/Brock battle model, the
Torrent and `ball_delay` probes, LeafGreen and per-scene `text_hold`, and the cheap
seeds 64-255 scan.
