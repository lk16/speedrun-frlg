# 2026-08-14 17:05 — tier 2 passed: the 38862 run is accepted

The host drained the queue at 16:59 CEST. `route-38862f-8ee04c5b8bfc` — the committed
38862-frame route, FireRed/Squirtle on `seed_delay` 27, with the solver'd rival battle —
came back **pass**: BizHawk 2.11.1 replayed all 38862 frames to the ledger's final ram_hash
(`868413a643bea3d7e0aa15bbb41d448f46301c00`, which is the `brock` segment's) with the
per-frame gRngValue probe matching every frame
(`verify/results/route-38862f-8ee04c5b8bfc.json`, `fast+headless`, 67s). Queue is empty again.

Checked before widening the ledger, same as the last two times: re-running
`frlg route export --target defeat-brock` on the committed logs reproduces the result's
`ilog_sha1` (`8ee04c5b8bfcfedcf87c1acf7a9a7b1e05b5066d`), so the pass belongs to exactly the
logs in `route/defeat-brock/logs/`. The re-export's `.bk2` hashed `ab661355…` against the
queued movie's `57392ed5…` — the same zip-metadata instability `docs/rival-1/route.md`
warns about. The ilog digest is the identity; the bk2 hash only names the bytes replayed.

All 19 segments' `tier2` fields now cite this result, `route.md`'s status line and Tier 2
section say passed, and `docs/sandbox.md`'s "defeat-brock's 38950" was stale.

## What the pass actually buys

The 88 frames came from a constraint-solver pass on the lab rival battle
(`journal/2026-08-14-16-00-solver-on-the-rival-battle.md`): the winning plan was chosen
against `frlg_battle`'s *model* of the fight, then played back through libmgba. A model-chosen
plan is exactly the kind of thing that can be right on tier 1 and wrong on real timing, so
this verdict is worth more than a routine re-verification — BizHawk agreeing frame-for-frame
on the gRngValue probe across the whole 38862 means the solver's 2445-frame battle is a real
battle, not a fit artifact.

Two notes, neither about the route:

1. **67s for 38862 frames** sits where the fit from last time predicts (~790 fps steady-state
   behind ~19s of fixed startup: 19 + 38862/790 ≈ 68s). It is *not* a test of that fit — at
   38862 vs 38950 frames it is the same point measured twice, and it agreeing tells us the
   runner is stable, nothing about the curve. A short movie replayed headless would still be
   the cheap third length; the 9658 rival-1 movie is the obvious candidate.
2. **The artifacts-side Lua override is still there and still byte-identical** to
   `tools/verify-runner.lua` (`diff` clean, checked again this session) — the runner announced
   it at the top of this run. The fold-back-and-delete condition from
   `docs/rival-1/route.md` has been met since 12:08; deleting
   `$FRLG_ARTIFACTS/verify/verify-runner.lua` is a host-side action, still pending, and until
   it happens every result line reads as if it ran on an unreviewed script.

Nothing in route.md's "not optimised" list moves: the Sammy/Brock battle model, the Torrent
and `ball_delay` probes, LeafGreen and per-scene `text_hold`, and the cheap seeds 64-255 scan
all stand where the solver session left them.
