# 2026-08-13 09:00 — session state (written for continuity)

Where the semi-naive build stands, precisely, so any session can pick this up cold.

## Running right now

`debug-run-rest` (frlg-route example) validating brock segments 4.. (14-tutorial onward)
from `$FRLG_ARTIFACTS/states/route-defeat-brock/13-deliver.state`, with
`DEBUG_STATES=<scratchpad>/dbg-states` saving a checkpoint per passed segment. Log:
`<scratchpad>/rest3.log`. This is the first run with the forest routed through Sammy's
corridor (waypoints `Near(forest,7,24)` → `Near(forest,5,20)` → north entrance via (5,10)).

## Proven by real builds (text_hold 4, turn_hold 2, Squirtle, FireRed)

01-boot..13-deliver, ending frame 19291. Rival seed wins 128/128 start delays (battle 2675
frames). Checkpoints for all of these in `$FRLG_ARTIFACTS/states/route-defeat-brock/`.
14-tutorial (6161f) and 15-to-forest (2353f) passed in checkpoint runs, not yet in a full
build.

## The plan from here (user-approved: finish semi-naive, then optimize)

1. rest3 validates 16-forest, 17-to-gym, 18-brock. If 18-brock's fight is unwinnable at
   the arrived level, add exp (Camper Liam in the gym is the cheapest add) and revalidate
   from the dbg-states checkpoint.
2. Clean full build from power-on: `frlg route build --target defeat-brock --starter
   squirtle --text-hold 4` (writes `route/defeat-brock/`), then
   `frlg route verify --target defeat-brock --write`, commit logs+ledger, update
   `docs/defeat-brock/route.md` segments table, journal the numbers.
3. Queue tier 2: `frlg route export --target defeat-brock`.
4. Optimization, in expected-value order:
   - starter × text_hold parallel builds scored on total frames (Bulbasaur may beat
     Squirtle if its seed family is friendlier; Liam detour changes the compare);
   - flee/walk bloat: the committed logs carry bump frames and detours — re-walk legs
     with tightened searches;
   - wild-model emulator proof (frlg-mon `wild.rs`) against the committed grass logs,
     then model-driven encounter routing (pick paths/steps against the second LCG
     instead of flee-tunneling);
   - starter IV/nature manipulation at the ball (4 rolls, frame dial) once the battle
     engine prices Brock turns.

## Sharp edges already learned (do not re-learn)

- Scene-end predicate must be `!field_controls_locked && player_can_step` (script lock).
- `gBattleOutcome` stale until first action menu.
- Search nodes must key on `Observer::wild_key`; battle-bound edges are blocked edges;
  best-effort targets the battle frontier; forced walking tunnels across battles while
  the post-battle cooldown is fresh.
- The forest north-exit pocket connects ONLY via Sammy's corridor (west, row 22).
- Whole seed families can lose a battle at every start delay; `text_hold` moves the
  naming-exit reseed, `turn_hold` only slides within the searched window.
