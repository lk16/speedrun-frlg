# 2026-08-14 00:30 — the seed sweep lands: 45276 frames, −3867

The day's plan (measure → seed dial → parallel builds) closed its loop:

- **`seed_delay` sweep**: six candidates (title-screen idles 27, 6, 28, 13, 9, 38,
  ranked by simulating each wild seed's rate-test stream) built end-to-end in parallel,
  one core each, ~2.5 h wall for all six. Results: **38 → 45276**, 6 → 47626,
  27 → 47884, 9 → 48188, 28 → 49512, 13 lost its forest fight at the old 48-delay
  width; committed baseline 49143. The simulation ranked imperfectly (27 modeled best)
  but every one of its picks that finished beat the baseline — as a *filter* it works.
- **Why 38 wins**: its forest is 10583 vs 13764 — and not mainly through fewer flees
  (it actually has 11 to the baseline's 10). Its stream let the walker route past
  **Rick's sight line**, deleting a 4327-frame fight, and its crossings walk tighter.
  Squirtle reaches Brock at **L7** (rival + Sammy exp only), learns Bubble mid-Sammy,
  and still 2-turns Brock (plan `[98]`, 158/192 start delays win) — the L9-vs-L7
  question the old route worried about is answered: L7 suffices, at least on a stream
  this shape.
- **Baseline rebuild reproduced 49143 exactly** (85 min) — builds are deterministic,
  so a sweep's numbers are trustworthy.
- **`win_battle` got checkpointed** (resume candidates from per-menu savestates instead
  of replaying from the intro): verified move-for-move equivalent on the committed
  Sammy fight, and the final seed-38 build took ~55 min against the sweep's ~2.5 h.
  fight-lab also measured stage-1 width: Rick's optimum sat at delay 111 (−165 frames,
  out of width-48's reach), Sammy's inside 48 — en-route fights now search 192.
- The winner is **tier-1 verified from reset and committed**; tier 2 queued as
  `route-45276f-d4bee65f9371` (the older 49143 request is superseded).

Open right now: the **no-heal probe** — this run reaches the forest exit at 20/23 HP
(no poison), so `heal-pewter`'s 1759 frames buy 3 HP; the probe walks from the
forest-exit state straight to Brock and re-runs the 192-delay search. If Brock falls,
the segment list loses a segment on the next build.
